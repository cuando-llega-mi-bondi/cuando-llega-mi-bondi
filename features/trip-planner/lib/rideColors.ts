/**
 * Color del k-ésimo tramo de colectivo de un itinerario. El timeline
 * (TripResultsSheet) y los segmentos del mapa (itineraryMapPayload) indexan
 * igual: si divergen, el usuario ve colores distintos para el mismo tramo.
 */
export const RIDE_COLORS = ["#0ea5e9", "#a855f7", "#f59e0b", "#10b981"];
